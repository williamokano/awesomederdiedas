{
  "name": "sync-exercises",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Converts german-learning exercises.yml into the JSON assets bundled with the app.",
  "scripts": {
    "sync": "node sync.mjs"
  },
  "dependencies": {
    "esbuild": "^0.25.0",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  }
}
